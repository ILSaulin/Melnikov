$(".regular").slick({
    // для точек снизу
    dots: true,
    infinite: true,
    // slidesToShow: 3,
    // slidesToScroll: 3 
    slidesToShow: 1,
    slidesToScroll: 1,
    // Для авто перемотки
    autoplay: true,
});